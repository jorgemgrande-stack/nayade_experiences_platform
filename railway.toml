[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "node scripts/migrate.mjs && node --trace-exit --trace-warnings dist/index.js 2>&1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
